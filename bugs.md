添加商品时候，会在库存中显示每个仓库都会添加这种商品。这个逻辑有问题，是不是应该现在只能添加库存中存在的商品，也就是，先入库，才能添加商品。那么这个初始库存的字段是不是也不需要了。
分类管理中的排序是否有必要，ID是不是可以直接替代。
促销中是不是应该，先选择促销类型，再弹出相应的处理，比如选择折扣后再弹出折扣率。还有就是积分到底是怎么积的。
供应商的信用等级现在我输入什么都可以，这不行，是不是应该规定一定的范围，供选择。
合同管理是不是应该与订单采购放在一起，把这两个合并。而且商品的成本价是不是就是，采购时的单价。而且这里面还涉及到入库的管理，现在新增入库根本添加不了，入库的商品是需要我们填写的，这不能只给下拉框了吧。这里需要好好构思一下。
为什么评价管理的评价删除不了，显示404 Not Found: The requested URL was not found on the server. If you entered the URL manually please check your spelling and try again。
安全库存没有设置的入口呀。
出库是什么意思呢，从仓库拿到货架上吗？有必要设置出库吗。而且出库的状态显示待处理，但是没有审批的入口呀。
收银的时候是不是应该把折扣之类的促销先显示出来，再结算；而不是点了结算才显示那些东西
销售订单不需要有新建入口吧，只用收银这一个接口就行了。
退货的时候，当输入的应该是订单编号吧，而且输入编号后就应该显示顾客买了什么东西，然后直接从这里面选择要退什么，退多少，总计退款金额，而不是手动计算
收银记录也不能手动添加了吧，而是通过pos收银这个唯一接口
税务与预算接口有很大问题，添加的东西都没法审核和确认。税务申报，一直显示的是未支付，那怎么支付呢
创建备份后，备份的是什么东西，放在哪里。